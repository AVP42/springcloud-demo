#使用这种方式，要使用这种方式访问"http://host:port/actuator/hystrix.stream"
management.endpoints.web.exposure.include=hystrix.stream
#加上下面这个配置，可以保留"http://host:port/hystrix.stream",因为默认是actuator
#management.endpoints.web.base-path=/
